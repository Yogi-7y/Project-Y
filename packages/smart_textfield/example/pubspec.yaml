name: example
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  smart_textfield:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints_y:
    path: ../../lints_y

flutter:
  uses-material-design: true
